El usuario envia un video por telegram --> El bot de telegram avisa al usuario que se esta procesando su video --> el archivo a un server backend levantado con socketserver --> el server con multiprocessing o multithreading realiza el interpolado (ver si hacerlo lineal o adyacente) --> se reconstruye el video --> se devuelve el video al bot de telegram --> se devuelve al usuario.